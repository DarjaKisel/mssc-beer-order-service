spring:
  cloud:
    discovery:
      enabled: true
    config:
      discovery:
        service-id: config
      fail-fast: true
      username: MyUserName
      password: MySecretPassword

eureka:
  client:
    service-url:
      defaultZone: http://netflix:eureka@localhost:8761/eureka